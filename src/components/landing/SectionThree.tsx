import React from 'react';

/**
 * Section Three - Intentionally empty component for future content expansion.
 * Full width, white background, min-height 80vh.
 */
export const SectionThree: React.FC = () => {
  return (
    <section
      id="section-three"
      className="w-full min-h-[80vh] bg-white"
      aria-label="Section Three"
    />
  );
};
